version: '3.8'

services:
  # 🏰 Guild Sync Service
  guild-sync:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: poolparty-guild-sync
    restart: unless-stopped
    
    # Environment configuration
    env_file:
      - .env
    
    environment:
      - NODE_ENV=production
      - DATABASE_PATH=/app/data/guild-sync.db
      - HEALTH_CHECK_PORT=3001
    
    # Volume for persistent SQLite database
    volumes:
      - guild_data:/app/data
      - ./logs:/app/logs
    
    # Network configuration
    networks:
      - guild_network
    
    # Health check
    healthcheck:
      test: ["CMD", "node", "src/health-check.js"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    
    # Resource limits
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '0.5'
        reservations:
          memory: 256M
          cpus: '0.1'
    
    # Logging configuration
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "3"
    
    # Labels for management
    labels:
      - "com.poolparty.service=guild-sync"
      - "com.poolparty.version=1.0.0"
      - "traefik.enable=false"

  # 📊 Optional: Monitoring with Watchtower (auto-updates)
  watchtower:
    image: containrrr/watchtower:latest
    container_name: poolparty-watchtower
    restart: unless-stopped
    
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    
    environment:
      - WATCHTOWER_CLEANUP=true
      - WATCHTOWER_POLL_INTERVAL=3600  # Check for updates hourly
      - WATCHTOWER_INCLUDE_STOPPED=true
      - WATCHTOWER_REVIVE_STOPPED=false
    
    networks:
      - guild_network
    
    labels:
      - "com.poolparty.service=watchtower"

  # 📈 Optional: Prometheus monitoring
  # Uncomment if you want metrics collection
  # prometheus:
  #   image: prom/prometheus:latest
  #   container_name: poolparty-prometheus
  #   restart: unless-stopped
  #   
  #   ports:
  #     - "9090:9090"
  #   
  #   volumes:
  #     - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml
  #     - prometheus_data:/prometheus
  #   
  #   command:
  #     - '--config.file=/etc/prometheus/prometheus.yml'
  #     - '--storage.tsdb.path=/prometheus'
  #     - '--web.console.libraries=/etc/prometheus/console_libraries'
  #     - '--web.console.templates=/etc/prometheus/consoles'
  #   
  #   networks:
  #     - guild_network

# 💾 Persistent volumes
volumes:
  guild_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ./data
  
  # prometheus_data:
  #   driver: local

# 🌐 Networks
networks:
  guild_network:
    driver: bridge
    ipam:
      config:
        - subnet: 172.20.0.0/16